# Data Model

This directory contains the reference for the backend data model.

## Schema

The complete database schema is defined in the `schema.sql` file. This file serves as the single source of truth for all data structures used throughout the Krabby application suite. When developing client-side applications, refer to this schema to define types and interfaces for state management (e.g., Redux Toolkit slices) to ensure consistency and type safety.